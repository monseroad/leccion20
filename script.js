// tu codigo va aca

function callbackPromedio (a, b, c, callback){
    var p = (a + b +c)/3;
	callbackPromedio = a.toString() + "+" + b.toString() + "+" + c.toString + "/" + 3 + "=" + p.toString ;
   
     return callbackPromedio; 
}

  